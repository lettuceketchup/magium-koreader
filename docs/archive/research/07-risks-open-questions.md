# 07 — Risk register & open questions

- **Status:** living (updated every phase)
- **Last updated:** 2026-09-08 (**Phase 7 licensing run:** OQ-004 + OQ-005
  closed — the family released the story text CC BY 4.0 for exactly this
  purpose, so no permission request is needed; the port's code is licensed
  AGPL-3.0-or-later to match KOReader, story text stays CC BY 4.0. See
  [ADR-008](../../decisions/ADR-008-license-and-distribution.md),
  [`08-licensing.md`](08-licensing.md). **Every OQ is now closed.** Prior same
  day — **Phase VIII close-out:** OQ-007 closed —
  owner judged the e-ink choice→page loop acceptable on the real device;
  OQ-011 closed — measured ~15 ms/render for the 2044-clause outlier, already
  mitigated by parse-time DNF caching; OQ-013 closed — the custom paginated
  reader shipped in Phase I. **Every OQ is now closed or deferred; the port is
  feature-complete.** Prior: 2026-08-31 **Scope update:** OQ-004/OQ-005 deferred —
  personal-use-only scope confirmed by owner, see
  [ADR-003](../../decisions/ADR-003-defer-licensing-distribution.md). Prior:
  **Phase 6 closed:** table cleanup pass — four
  rows (OQ-001, OQ-009, OQ-011, OQ-012) had lost their Blocking?/Status/
  Resolution column split somewhere across earlier edits, collapsing Status
  and Resolution into one cell and rendering Resolution empty; re-split below,
  no content changed. New §"Blocking status after Phase 6" cross-references
  [`06` §3](06-approach-comparison.md#3-blocking-open-questions-63) /
  [ADR-002](../../decisions/ADR-002-porting-approach.md) for which open items
  actually gate the approach decision (none do) vs. the project overall
  (OQ-004). Prior: Phase 5 closed OQ-006 on fidelity, OQ-001 further narrowed
  (memory ~closed, now under two LuaJIT builds), **OQ-002's data/API-fit half
  closed** — a real KOReader build confirms the widget accepts the data —
  refresh feel stays OQ-007, final UI shape splits off as **new OQ-013**
  (fullscreen + paginated vs. `TextViewer`'s padded scrolling dialog, from
  owner review of spike 04's screenshots), OQ-012 gains a note that a cloud
  session *can* build/run the emulator after all, with a documented fix — see
  [`docs/spikes/`](../spikes/))
- **Phase:** all
- **Related:** every other research doc; [`../decisions/`](../../decisions/), esp.
  [ADR-002](../../decisions/ADR-002-porting-approach.md) (Phase 6 approach decision)
  and [ADR-003](../../decisions/ADR-003-defer-licensing-distribution.md)
  (licensing/distribution deferred)

> This is the single home for open questions. Elsewhere, reference them by ID
> (`OQ-007`), never restate. Closed questions stay here with their resolution.

## Blocking status after Phase 6

[ADR-002](../../decisions/ADR-002-porting-approach.md) picked **Option A
(standalone KOReader plugin, Lua engine)**. Checked against every open row
below ([`06` §3](06-approach-comparison.md#3-blocking-open-questions-63) has
the full reasoning): **none of the still-open questions are strong enough to
change that decision, and none of them are currently blocking anything.**
They split into two groups:

- **~~Deferred~~ → closed (Phase 7, 2026-09-08):** **OQ-004** (redistribution
  permission) and **OQ-005** (license) were deferred under
  [ADR-003](../../decisions/ADR-003-defer-licensing-distribution.md) while the
  scope was personal-use-only. The owner then decided to release the port
  freely (non-commercial), which is the revisit trigger ADR-003 named, so
  Phase 7 ran: the story text is CC BY 4.0 (no permission request needed), the
  port's code is AGPL-3.0-or-later. [ADR-008](../../decisions/ADR-008-license-and-distribution.md)
  supersedes ADR-003; [`08-licensing.md`](08-licensing.md) is now `stable`.
- **Narrowed an implementation detail inside Option A, didn't gate it —
  now all closed:** OQ-001's parse-time tail (spike 06, Milestone 0),
  OQ-007 (e-ink feel — Phase VIII device pass, acceptable), OQ-011 (490 KB
  condition — Phase VIII, ~15 ms/render), OQ-013 (pagination widget — built
  in Phase I). See each row's Resolution.

**Phase 8 disposition:** none of these four are answerable on paper — each
needs either real device time or actual code — so [`09-roadmap-effort.md`](09-roadmap-effort.md)
schedules them as concrete implementation work rather than leaving them
open-ended: OQ-001's tail is Milestone 0 (a pre-flight on-device parse-timing
gate, before Phase I starts); OQ-013 is resolved by design in Phase I (the
custom pagination widget is built from the start, not bolted on later);
OQ-007 and OQ-011 are Phase VIII (polish) line items. This satisfies the
design doc §11 exit criterion ("every OQ-NNN closed or explicitly deferred
with a reason") for these rows without overstating them as closed.

## How to use

- Add a row when a question arises. Assign the next `OQ-NNN`.
- **Venue** = where the answer comes from: `spike` · `magium-dev source` · `web build` ·
  `KOReader GH` · `MobileRead` · `r/koreader` · `Magium Discord` · `intfiction.org` ·
  `<plugin author>`.
- **Blocking?** = does an approach decision or the feasibility verdict depend on it.
- On close: set Status = `closed`, fill Resolution with a link to the finding/ADR.

## Open questions

| ID | Question | Why it matters | Venue | Blocking? | Status | Resolution |
|---|---|---|---|---|---|---|
| OQ-001 | What is the *Lua-side* memory cost of holding the full parsed story, and is a cold parse of 54 files fast enough not to stall launch? | Chooses parse-all vs. lazy-per-chapter vs. build-time preprocess. | spike B + D | no — mitigations scoped ([`04` §4](04-constraints-budget.md#4-runtime-parsing-vs-build-time-preprocessing-34), [ADR-002](../../decisions/ADR-002-porting-approach.md)) | narrowed — memory closed (🟢), parse-time tail still open | Device has **~957 MB RAM / ~500 MB available**, KOReader only ~33 MB ([`00`](00-overview.md), [`04`](04-constraints-budget.md)). ~17–30 MB resident story fits comfortably. **Phase 3:** memory does not force the parse-strategy choice (§4 of [`04`](04-constraints-budget.md#4-runtime-parsing-vs-build-time-preprocessing-34)). **Phase 5** ([spike 03](../spikes/03-full-corpus-memory-parse/FINDING.md)): a real **LuaJIT** measurement (not just the V8 estimate) of the full 54-file parse, confirmed under **two** LuaJIT builds — stock apt: **11.54 MB** heap delta / 112–128 ms parse; koreader-base's own bundled build (once a working `./kodev build` was obtained in-session, see OQ-012): **11.48 MB** / 184–205 ms. Both still desktop x86, not the Kindle's ARM core. Memory: effectively closed, strongly 🟢. Parse time: still an extrapolation — needs the real device or the owner's WSL2 emulator. **Phase 6:** does not gate the approach choice — [ADR-002](../../decisions/ADR-002-porting-approach.md) picked Option A regardless, with the parse-all-vs-lazy-cache call left as an early implementation-phase gate against real ARM timing. |
| OQ-002 | Is there an existing KOReader widget combo that gives acceptable prose + choice-list UX without building custom widgets? | Effort + approach B feasibility. | spike A / KOReader GH | yes | **API/data fit closed — yes; final chrome & navigation still open, see OQ-013** | Platform read ([`03` §3](03-koreader-platform.md#3-ui-toolkit-inventory-23)): `ScrollTextWidget`/`TextViewer` (prose) + `ButtonTable`/`ButtonDialog`/`Menu` (choices) + `KeyValuePage` (stats) cover the need with no missing capability; `kbarni/frotz.koplugin` ships a working fullscreen "styled transcript + input row" widget. **Phase 5** ([spike 04](../spikes/04-ui-plugin-skeleton/FINDING.md)): a `TextViewer` + `buttons_table` plugin, API-grounded against real `v2026.07.1` source, was actually **run** under a real `./kodev build` + `xvfb-run ./kodev run --simulate=kindle-paperwhite` in this session — loads with zero errors, both hard-coded scenes render correctly (real prose, correct chapter header, working choice buttons), navigation works, screenshotted. **The widget *accepts* this data shape without error and is trivial to drive — confirmed.** But reviewing those screenshots surfaced that `TextViewer` itself is the wrong final choice: it's a padded dialog (screen − 30px, rounded frame, titlebar+close button), not fullscreen, and its prose area continuously scrolls with no page concept — neither matches "fullscreen, like the web version" with clean paginated navigation. Split off as **OQ-013**, which is the real remaining UI-shape question; OQ-007 (e-ink refresh feel) is unaffected either way and stays open, needs the owner at a real (or WSL2-emulated, same instant-refresh caveat) device. |
| OQ-003 | Does any existing KOReader plugin already play Twine/Ink/gamebook content offline? | Enables approach B or C. | prior art / KOReader GH | yes | **closed — no** | [`05` §2](05-prior-art.md#2-existing-koreader-game--non-book-plugins-42): full `awesome-koreader` + KOReader-ecosystem survey finds IF interpreters (`frotz.koplugin` et al.) and generic puzzle-game plugins, but zero CYOA/gamebook/narrative-choice plugins (F-30). Rules out approach B (extend an existing plugin) as a shortcut. |
| OQ-004 | Does the family's permission for the community recreations extend to a further e-reader port and redistribution of story text? | Legal basis for distribution. | Magium Discord | no | **closed — yes, no permission request needed** | **Phase 7, 2026-09-08 ([`08` §2](08-licensing.md#2-story-text-permission-chain-72), [ADR-008](../../decisions/ADR-008-license-and-distribution.md)):** the question is moot — `raduprv/Magium`'s README states verbatim *"All the data (text and such) is released under CC BY 4.0 license"*, and the release was made by the author's brother with the family's permission for the explicit purpose of "porting the game to other languages, and hopefully finishing it too." CC BY 4.0 **is** the grant; redistributing the story text (with attribution + a note that it was reformatted) needs no further sign-off. The ADR-003 deferral assumed the permission chain was murky; confirming the CC BY 4.0 licence dissolved that. Discord outreach is now courtesy, not a blocker. |
| OQ-005 | Which upstream license governs a port — MIT (from `magium-dev` code) or does bundling KOReader-side make it AGPL? | Repo license choice. | intfiction/legal reading | no | **closed — AGPL-3.0-or-later for the port's code; story text stays CC BY 4.0** | **Phase 7, 2026-09-08 ([`08` §3/§5](08-licensing.md#3-koreaders-license-and-plugin-implications-73), [ADR-008](../../decisions/ADR-008-license-and-distribution.md)):** a `.koplugin` is `require`d into KOReader's Lua state, which has a plausible "single program / derivative of an AGPL work" reading. Licensing the port **AGPL-3.0-or-later** matches the host and removes the ambiguity at no practical cost — the MIT engine code composes into an AGPL aggregate cleanly (one-way permitted; MIT notice retained in [`THIRD-PARTY-NOTICES.md`](../../../THIRD-PARTY-NOTICES.md)). The bundled Magium story text is a separately-licensed data component and stays CC BY 4.0. `LICENSE` (AGPL-3.0) added this phase. |
| OQ-006 | Can `.magium` conditions/stats be faithfully represented in Twine/Ink, or is fidelity lost? | Viability of approach C. | spike C | no | **closed — fidelity not lost, but doesn't change the verdict** | [`05` §3](05-prior-art.md#3-twine--ink--choicescript-players-on-constrained-hardware-43): no existing Twine/Ink/ChoiceScript player runs on e-ink hardware today (F-27) — evidence against approach C independent of the fidelity question; Ink (MIT, portable runtime) flagged as the more promising conversion target for spike C over Twee if approach C is pursued at all. **Phase 5** ([spike 05](../spikes/05-magium-to-ink/FINDING.md)): converted `ch1.magium` (12 scenes) to Ink, compiled + played via `inkjs`'s in-process JS compiler (no `inklecate`/.NET needed) — conditions and `set()`-equivalent state convert **losslessly**, checked against the oracle goldens. Gaps found are orthogonal to fidelity: achievements/`special:` hooks have no Ink primitive (host-script territory regardless of engine), cross-chapter nav needs multi-file handling (one-chapter scope, not attempted). Net: the fidelity concern is resolved favorably, but doesn't change Phase 4's read against approach C — deployability (F-27) is still the blocker. |
| OQ-007 | E-ink refresh feel for a "tap choice → new page of prose" loop — acceptable or sluggish/ghosting? | Core UX viability. | spike A | yes | **closed — acceptable** | Platform read ([`03` §6](03-koreader-platform.md#6-e-ink-specifics-26)): the tools exist — `"ui"` (no-flash) for scene swaps/scrolling, occasional `"full"` to de-ghost, MTK fast-mode is force-enabled on `KindlePaperWhite6` so `"ui"` stays quick. **Phase 5** ([spike 04](../spikes/04-ui-plugin-skeleton/FINDING.md)): spike A's plugin was run under KOReader v2026.07.1 — widget mechanics confirmed (OQ-002) — but SDL renders instantly on any non-e-ink display, so refresh feel was never observable there. **Phase VIII (2026-09-08, [spec](../../specs/2026-09-08-phase-viii-polish.md)):** owner ran the choice→page loop on the real Paperwhite 12 and judged it **acceptable** — a little ghosting is present but explicitly not worth tuning for a personal-use build (no optimization requested "unless people ask or I notice an issue"). `magium.koplugin/ui/refresh.lua`'s policy — `"ui"` on page-turn / scene-swap, `"full"` every 6th scene to de-ghost, `"full"` on open — **ships unchanged**. `crash.log` clean across the pass (parse 2054 ms / 2159 scenes, ~15 page-turns, 4 choices, an achievement toast, saves open/close, suspend + reader-close flush). Reopen if it ever grates in real play — the four `return` values in that one 14-line file are the knob. |
| OQ-008 | Does the `magium-dev` parser have latent bugs on multi-digit `set()` values or quoted choice labels that a port should fix rather than copy? | Correctness of a faithful port. | magium-dev source / web build | no | **mostly resolved** | Full corpus scan ([`02` §3–4](02-magium-format-spec.md#3-construct-corpus-task-111)): **no** multi-digit `set()` exists (the 1-digit regex is safe as-is, but a port should *assert* not truncate); `choice(""spoken"")` doubled-quote labels are common (809×) and handled correctly by the greedy capture. Real latent hazards catalogued as [`02` §4](02-magium-format-spec.md#4-parser-risk-list) R1–R10 (unanchored regexes, `startsWith` traps, single-`replace` paren stripping) — none triggered by current data. |
| OQ-009 | Does KOReader v2026.07.1 stay stable on this device under a memory-heavier plugin (holding the story + doing IO)? | A flaky base platform blocks all testing. | spike A/D on-device | partially | mostly resolved | KOReader v2026.07.1 **release** build runs fine on FW 5.19.5 (owner). #13307-class launch crashes not seen here. **Phase 5** ([spike 04](../spikes/04-ui-plugin-skeleton/FINDING.md)): a real `./kodev build` ran headless with the story plugin loaded alongside every bundled plugin, zero errors — a first, emulator-only data point for "under plugin load." Still open: sustained load on the real device (not just emulator startup) — check during implementation-phase testing. |
| OQ-011 | Is per-render condition evaluation fast enough on the Kindle CPU, given the outlier `b3ch4a.magium:251` — a single ~490 KB `choice … if (…)` condition with **2044 OR-clauses** (and sibling Average-Joe scenes with similar pre-expanded DNFs)? | If re-splitting/evaluating that string on every visit to `B3-Ch04a-Stats-spent` stalls the UI, the port needs condition caching, a targeted pre-compile, or a special-case — all still inside Option A ([ADR-002](../../decisions/ADR-002-porting-approach.md)), not a reason to adopt build-time preprocessing wholesale (candidate D). | spike B | no — mitigable | **closed — not a stall** | Surfaced by Phase 1 ([`02` §4 R7](02-magium-format-spec.md#4-parser-risk-list), [`01` F-07/§11](01-magium-analysis.md#11-parsed-story-size--memory-footprint-task-112)). **Phase 3** ([`04` §3 row 4](04-constraints-budget.md#3-budget-table-33)): ordered mitigations = (1) memoise the parsed DNF for that scene id, (2) special-case as a direct stat comparison, (3) targeted build-time pre-compile. **Phase VIII ([spec](../../specs/2026-09-08-phase-viii-polish.md) §3, [`spec/engine/condition_perf_spec.lua`](../../../magium.koplugin/spec/engine/condition_perf_spec.lua)):** mitigation #1 turned out to be *already the architecture* — `engine/parser.lua` calls `parse_conditions` at **parse time** and stores the DNF table on the scene; `engine/conditions.lua:eval` only walks the pre-built table per render. The 490 KB string is split into its 2044 AND-groups exactly once, inside the ~2.2 s cold preload spike 06 already measured. Measured render cost of `B3-Ch04a-Stats-spent` (condition walk included): **2.6 ms/render on dev x86 LuaJIT** → **~15 ms on the Kindle** at spike 06's directly-measured ×5.6 x86→device factor — imperceptible. **No mitigation code**; the bench ships as a permanent regression tripwire (loose 50 ms/render x86 budget; fails loud if a change ever re-parses the string per render or if the data drops the outlier). |
| OQ-010 | Exact KOReader version, release channel, firmware, and RAM on the owner's device? | Every constraint estimate depends on these. | on-device (Phase 0.1) | — | **closed** | FW **Kindle 5.19.5** (4794310058); KOReader **v2026.07.1** release (`kindlehf`); **956.9 MB RAM** (497.5 available); 10.6 GB free storage; KOReader idle RSS ~32.7 MB. Recorded in [`00-overview.md`](00-overview.md). **LuaJIT tail closed (Phase 2):** `koreader-base` @ `6e4bc81` builds `LuaJIT/LuaJIT@3c4f9fe` (v2.1 branch), self-id `LuaJIT 2.1.ROLLING` / `LUAJIT_VERSION_NUM 20199`, upstream (not OpenResty) — [`03` §2](03-koreader-platform.md#2-lua-environment-22). |
| OQ-012 | What is the least-friction KOReader dev loop on Windows (the owner's OS)? The `kodev` emulator is Linux/macOS-only; options are WSL2 + X server, the `koreader/virdevenv` Docker image, extracting a Linux AppImage for pure-Lua work, or on-device-only (USB copy + `crash.log`). | Sets the iteration speed and therefore the effort estimate for all KOReader-facing work (Phase 8). | spike A / KOReader Docker docs | no — mitigable | resolved | **WSL2 / Ubuntu 24.04** — `./kodev build` + `./kodev run` working; **WSLg** gives a display with no X server (SDL `x11` driver). Needed ninja ≥1.13.2 + GNU make ≥4.4 in `/usr/local/bin` (Ubuntu 24.04 ships 1.11.1 / 4.3 → recursive-make thirdparty builds fail `read jobs pipe: Bad file descriptor`). Reproducible: [`reference/setup-koreader-wsl.sh`](../../../reference/setup-koreader-wsl.sh); notes in [`reference/koreader-notes.md`](../../../reference/koreader-notes.md). Build ~7 min. **Note (Phase 5):** a *cloud/remote* Claude Code session attempting the identical build initially hit a **different, unrelated** blocker — `./kodev fetch-thirdparty`'s GitHub **archive**-tarball downloads (`github.com/*/archive/*`, ~17 of ~50 thirdparty libs) returned 403 under that session's network egress policy — and first reported this as "a cloud session can't build the emulator at all." **That was corrected the same session:** the block turns out to be narrower — plain `git clone` and `github.com/*/releases/download/*` (release assets) are *not* blocked, only the archive-tarball endpoint is, for repos outside the session's attached scope. Patching those 17 fetches to `git clone` at the same tag (content-identical; uses the build's own pre-existing `DOWNLOAD GIT` mechanism) let `./kodev build` complete end to end, and the emulator ran headless via Xvfb — see [spike 04](../spikes/04-ui-plugin-skeleton/FINDING.md) for the full patch and [`reference/setup-koreader-cloud-session.sh`](../../../reference/setup-koreader-cloud-session.sh) for the reproducible recipe. This does not reopen OQ-012 (specifically about the owner's own Windows machine, resolved above); it means a cloud agent session *can* build/run the emulator, at the cost of this one documented patch — the owner's own WSL2 build remains unaffected and unnecessary for this specific validation, since it's now been done here. |
| OQ-013 | Should the final UI be a **custom fullscreen widget with real pagination** (screen-sized page chunks, page-turn refresh, a page indicator) rather than an off-the-shelf `TextViewer` (padded dialog, continuous scroll)? | Determines the widget/rendering architecture for the reading screen — affects effort (custom pagination logic vs. reuse), and directly targets the owner's stated goals: fullscreen chrome "similar to the Web version", and avoiding continuous-scroll's e-ink ghosting + poor navigability. | spike A (done) / a future spike | yes — for UI-design approval, not for feasibility | **closed — custom widget, built in Phase I** | **Resolved by Phase I:** `magium.koplugin/ui/reader.lua` is a bespoke fullscreen `FrameContainer` with real pagination — screen-sized pages, a `N / total` indicator, per-page-turn refresh, no continuous scroll, no `TextViewer` dialog chrome. Owner sign-off Phase I (2026-09-01) and confirmed acceptable on every device pass since, including the Phase VIII e-ink pass (2026-09-08). Later hardened for non-PW12 viewports in Phase VI (`test-ui-matrix`, 4 device profiles). Raised from owner review of [spike 04](../spikes/04-ui-plugin-skeleton/FINDING.md)'s screenshots (2026-08-31). Confirmed source-side: `TextViewer` defaults to `screen_w/h − 30px` with a rounded dialog frame + titlebar/close button, not true fullscreen (`textviewer.lua:107-108,469-474`); its `ScrollTextWidget` prose area is continuous-scroll with no page-number/pagination concept (`scrolltextwidget.lua`). Neither gap is specific to this spike's widget choice — `frotz.koplugin`'s bespoke fullscreen `GameView` (the other cited prior art) is fullscreen but *also* scrolls (`StyledScroll`), so no cited KOReader prior art already does "fullscreen + paginated." A real pagination widget is buildable (`TextBoxWidget` exposes line/height measurement to chunk text into screen-sized pages, per [`03` §3](03-koreader-platform.md#3-ui-toolkit-inventory-23)) but wasn't spiked — this is new custom-widget work, not a reuse. Also worth checking once a build phase starts: most scenes are short (4880 paragraphs / 2159 scenes ≈ 2.3 avg, [`01` §11](01-magium-analysis.md#11-parsed-story-size--memory-footprint-task-112)) and may fit one screen without any scrolling *or* pagination once rendered in a true fullscreen frame (no titlebar/button-row/30px-margin overhead) — pagination mainly matters for the longer scenes, not a full rewrite of every screen. Feeds Phase 6 (approach comparison — this is a component of "parity ceiling"/effort, not a go/no-go factor) and Phase 8 (roadmap — the custom-pagination widget would be its own line item). |

## Retired / deferred

_(none yet)_
